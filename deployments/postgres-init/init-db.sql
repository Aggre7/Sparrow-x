CREATE DATABASE user_write;
CREATE DATABASE keycloak;
-- Grant privileges if necessary, though the default 'postgres' user should have them
-- For example:
-- GRANT ALL PRIVILEGES ON DATABASE passenger_write TO postgres;
